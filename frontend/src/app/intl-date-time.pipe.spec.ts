import { IntlDateTimePipe } from './intl-date-time.pipe';

describe('IntlDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new IntlDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
